{
  "name": "fileziper-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter fileziper-backend dev",
    "dev": "pnpm --parallel dev",
    "build": "pnpm -r build",
    "install-all": "pnpm install"
  }
}
